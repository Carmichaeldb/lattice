<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Lattice - Explore Topics</title>
    <style>
        body {
            font-family: 'Times New Roman', serif;
            font-size: 24px;
            font-weight: normal;
        }
        header {
            background-color: #f8f8f8;
            padding: 10px 0;
            border-bottom: 1px solid #e7e7e7;
        }
        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            overflow: hidden;
            text-align: center;
        }
        nav ul li {
            display: inline;
            margin-right: 20px;
        }
        nav ul li a {
            text-decoration: none;
            color: #333;
            font-weight: bold;
            padding: 14px 16px;
        }
        nav ul li a:hover {
            color: #017572;
        }
        #username {
            float: right;
            margin-right: 20px;
        }
        main {
            padding: 20px;
            text-align: center;
        }
        .topic {
        font-size: 24px;
        margin-bottom: 40px; /* Updated margin-bottom */
    }

    .grid-container {
        display: grid;
        grid-template-columns: repeat(3, 1fr); /* Updated minmax value */
        grid-gap: 20px;
        padding: 20px;
    }

    .grid-item {
        background-color: #D9D9D9; /* Slightly lighter shade of grey */
        border-radius: 40px;
        padding: 20px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1); /* New box shadow */
        margin-bottom: 20px;
        /* Add any other styles you wish to apply to grid items */
    }

    .grid-item-post {
        background-color: #fff;
        border: 1px solid #ddd; /* Border for each post */
        margin-bottom: 10px; /* Spacing between posts */
        padding: 10px;
        border-radius: 30px; /* Slightly rounded corners for posts */
    /* Add more styling for posts if needed */

    }

    @media (max-width: 768px) {
    .grid-container {
        grid-template-columns: repeat(2, 1fr); /* 2 columns on smaller screens */
    }
}

    @media (max-width: 480px) {
    .grid-container {
        grid-template-columns: 1fr; /* 1 column on very small screens */
    }
}


    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/explore">Explore</a></li>
                <li><a href="/create">Create</a></li>
                <li><a href="/search">Search</a></li>
                <li id="username">username</li>
            </ul>
        </nav>
    </header>
    
    <main>
        <div class="grid-container">
            <% topicsWithPosts.forEach(topicWithPosts => { %>
                <% if (topicWithPosts.posts && topicWithPosts.posts.length > 0) { %>
                    <div class="grid-item">
                        <h2><%= topicWithPosts.topic_name %></h2>
                        <% topicWithPosts.posts.forEach(post => { %>
                            <div class="grid-item-post">
                                <h3><%= post.title %></h3>
                                <p><%= post.description %></p>
                                <!-- Other details about the post -->
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            <% }) %>
        </div>
    </main>
    
    
</body>
</html>

